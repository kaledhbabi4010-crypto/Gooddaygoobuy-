name: gemini-real-android (Disabled / Manual Only)

on:
  workflow_dispatch:

jobs:
  disabled-placeholder:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Workflow disabled per user command to focus on Web AI Platform."

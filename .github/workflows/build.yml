KHALED-MASTER-PLAN.md   ← المواصفات
        ↓
bootstrap.ps1           ← ينفذ إنشاء المشروع
        ↓
KHALED-X.csproj         ← مشروع حقيقي
        ↓
src/                    ← الكود الحقيقي
tests/                  ← الاختبارات
        ↓
build.yml               ← يشغل كل شيء
        ↓
Build → Test → Verify
